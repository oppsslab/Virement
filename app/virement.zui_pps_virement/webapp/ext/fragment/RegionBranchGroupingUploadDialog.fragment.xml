<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified">
    <Dialog id="regionBranchGroupingUploadDialog" title="{i18n>UploadTemplate}" contentWidth="420px">
        <VBox class="sapUiTinyMargin">
            <Text text="{i18n>UploadRegionBranchGroupingHint}" />
            <u:FileUploader id="regionBranchGroupingFileUploader" name="regionBranchGroupingUpload" fileType="xlsx" placeholder="{i18n>ChooseFile}" class="sapUiSmallMarginTop" width="100%" />
        </VBox>
        <beginButton>
            <Button id="confirmRegionBranchGroupingUploadBtn" text="{i18n>Upload}" type="Emphasized" />
        </beginButton>
        <endButton>
            <Button id="cancelRegionBranchGroupingUploadBtn" text="{i18n>Cancel}" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
