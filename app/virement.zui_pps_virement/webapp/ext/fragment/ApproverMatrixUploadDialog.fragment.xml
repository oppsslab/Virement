<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified">
    <Dialog id="approverMatrixUploadDialog" title="{i18n>UploadTemplate}" contentWidth="420px">
        <VBox class="sapUiTinyMargin">
            <Text text="{i18n>UploadApproverMatrixHint}" />
            <u:FileUploader id="approverMatrixFileUploader" name="approverMatrixUpload" fileType="xlsx" placeholder="{i18n>ChooseFile}" class="sapUiSmallMarginTop" width="100%" />
        </VBox>
        <beginButton>
            <Button id="confirmApproverMatrixUploadBtn" text="{i18n>Upload}" type="Emphasized" />
        </beginButton>
        <endButton>
            <Button id="cancelApproverMatrixUploadBtn" text="{i18n>Cancel}" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
