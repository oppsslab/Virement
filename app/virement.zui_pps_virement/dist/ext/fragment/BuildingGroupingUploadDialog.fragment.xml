<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified">
    <Dialog id="buildingGroupingUploadDialog" title="{i18n>UploadTemplate}" contentWidth="420px">
        <VBox class="sapUiTinyMargin">
            <Text text="{i18n>UploadBuildingGroupingHint}" />
            <u:FileUploader id="buildingGroupingFileUploader" name="buildingGroupingUpload" fileType="xlsx" placeholder="{i18n>ChooseFile}" class="sapUiSmallMarginTop" width="100%" />
        </VBox>
        <beginButton>
            <Button id="confirmBuildingGroupingUploadBtn" text="{i18n>Upload}" type="Emphasized" />
        </beginButton>
        <endButton>
            <Button id="cancelBuildingGroupingUploadBtn" text="{i18n>Cancel}" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
