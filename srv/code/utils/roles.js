/**
 * Centralized application role names.
 * Keep in sync with the roles defined in ams/dcl/cap/basePolicies.dcl.
 */
const ROLES = {
  REQUEST_APPROVE: "REQUEST_APPROVE",
};

module.exports = { ROLES };
